import * as mocks from './todo/__mock__';
import * as interfaces from './todo/interfaces';
import * as models from './todo/models';
import * as services from './todo/services';

export { services, interfaces, models, mocks };

export default {
  interfaces,
  models,
  services,
  mocks,
};
