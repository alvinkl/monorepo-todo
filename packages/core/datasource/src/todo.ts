import * as fetch from './todo/fetch';
import * as interfaces from './todo/interfaces';
import * as models from './todo/models';
import * as services from './todo/services';

export { fetch, services, interfaces, models };

export default {
  fetch,
  interfaces,
  models,
  services,
};
